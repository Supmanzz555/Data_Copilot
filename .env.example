DATABASE_URL=postgresql://admin:changeme@db:5432/deep_insights
POSTGRES_USER=admin
POSTGRES_PASSWORD=changeme
POSTGRES_DB=deep_insights
DEBUG=false
DEBUG=false
GROQ_API_KEY="Your own groq api here"
# Optional: pick a smaller Groq model to reduce rate limits
GROQ_MODEL=llama-3.1-8b-instant
# Optional: cap completion size to reduce TPM usage
GROQ_MAX_TOKENS=256
# Jina Embeddings (https://jina.ai/embeddings) — required for KB load and semantic search
JINA_API_KEY=""
# Optional overrides (defaults match app/schema.sql vector(1024)):
# JINA_EMBEDDINGS_MODEL=jina-embeddings-v3
# JINA_EMBEDDING_DIMENSION=1024
